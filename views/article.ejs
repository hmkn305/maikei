<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Article Page</title>
    <script src="/send_url.js"></script>
  </head>
  <body>

    <%- include('header'); %>
    
  <main>

    <div class = "article">
     <% if (article.category === 'all') { %>
      <h1><%= article.title %></h1>
      <p><%= article.content %></p>
      <%} %>

     <% if (article.cotegory === 'limited') { %>
      <i>会員限定</i>
      <h1><%= article.title %></h1>
      <% if(locals.isLoggedIn) { %>
      <p><%= article.content %></p>
      <% } else { %>
        <div class ="article-login">
          <p>今すぐログインしよう！</p>
          <p>記事の続きは<br>ログインすると読むことができます</p>
          <a class="btn" href="/login">ログイン</a>
        </div>
      <% } %>
      <% } %>

    </div>
  </main>
  <footer>
    <a class="btn sub" href="/list">一覧に戻る</a>
  </footer>
  </body>
</html>